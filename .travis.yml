dist: xenial
language: python
python: 3.7
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - -m unittest discover -s pyviews -p *_tests.py

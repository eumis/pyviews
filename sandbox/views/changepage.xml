<?xml version="1.0"?>
<c:Container xmlns:tk="tkinter"
             xmlns:c="pyviews.view.core"
             xmlns:import="pyviews.modifiers.core.import_global"
             xmlns:bind="pyviews.modifiers.core.bind"
             
             import:Grid='pyviews.view.geometry.GridGeometry'
             import:show_view='sandbox.commands.show_view'>
    <tk:Button geometry='{Grid(0, 0, sticky="e")}'
               bind:Button-1="{lambda:show_view(self, 'messagebox')}">
        Message box
    </tk:Button>

    <tk:Button geometry='{Grid(0, 1, sticky="e")}'
               bind:Button-1="{lambda:show_view(self, 'counter')}">
        Counter
    </tk:Button>

    <tk:Button geometry='{Grid(0, 2, sticky="e")}'
               bind:Button-1="{lambda:show_view(self, 'scroll')}">
        Scroll
    </tk:Button>
</c:Container>